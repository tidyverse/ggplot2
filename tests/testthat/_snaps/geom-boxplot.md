# boxplots with a group size >1 error

    Code
      layer_grob(p, 1)
    Condition
      Error in `draw_group()`:
      ! Can only draw one boxplot per group.
      i Did you forget `aes(group = ...)`?

