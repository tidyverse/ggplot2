# binned scales only support continuous data

    Code
      ggplot_build(p)
    Condition
      Error in `scale_x_binned()`:
      ! Binned scales only support continuous data.

---

    Code
      ggplot_build(p)
    Condition
      Error in `scale_color_binned()`:
      ! Binned scales only support continuous data.

